
spring:
#  profiles: dev
  datasource:
    hikari:
      connectionTimeout: 20000
      maximumPoolSize: 5
    url: ${DB_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${RESOURCE_SERVICE_DB_USERNAME:postgres}
    password: ${RESOURCE_SERVICE_DB_PASSWORD:nimda}
  servlet:
    multipart:
      enabled: true
      max-file-size: 15MB
      max-request-size: 15MB

aws.s3:
  endpoint-url: http://localhost:4566
  region: us-east-1
  bucket-name: resources